'use client'

import AppLayoutMain from '@/components/layout/AppLayoutMain'

export default function Home() {
  // const route = useRouter()
  // useEffect(() => {
  //   route.push(ROUTE_PATH.admin.dashboard)
  // }, [route])
  return <AppLayoutMain title='Admin'>Admin</AppLayoutMain>
}
